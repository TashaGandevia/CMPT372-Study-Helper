// PostCSS configuration.
// Tailwind runs as a PostCSS plugin to generate utility classes; autoprefixer
// adds vendor prefixes for broader browser support.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
